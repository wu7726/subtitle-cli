# PRD —— B站合集字幕提取器 · Obsidian 知识库版

> 版本：v1.0（已确认，作为后续开发依据）　日期：2026-08-30
> 上游文档：`docs/产品设计文档.md`（工具本体 MVP+v0.2）、`docs/页面结构与交互设计.md`（本轮交互细化）
> 状态：范围经三轮问答确认——①直接写入 vault；②属性头+合集索引页；③旧字幕做迁移功能。

## 1. 产品目标和使用场景

**产品定位**：只供本人使用的本地工具，把 B站合集的字幕变成 Obsidian 知识库笔记。命令行工具本体已实现（M4），本期把「输出」从普通文件夹升级为「直接写入 Obsidian vault 的结构化笔记」。

**一句话目标**：粘贴一个合集（或合集内任意视频）链接 → 全部分集字幕转成带属性头的 Markdown 笔记直接写入 vault → 每个合集自动生成索引页串联全部分集，形成可搜索、可双链跳转的知识库。

**使用场景**：

1. **看完合集做归档**：看完一个几十集的合集，一次提取全部字幕入 vault，日后按合集/属性检索回顾。
2. **喂给 AI 总结**：笔记是纯文本段落，可直接复制或用 Obsidian 插件送入 AI 做摘要。
3. **旧字幕一次性入库**：把之前下载到 `output/` 的旧格式字幕批量转成新格式写入 vault（不联网）。

**用户与边界**：单用户、本地运行、个人学习用途；字幕内容归创作者/平台所有。

## 2. 整体页面框架和导航结构

- **形态**：单页本地网页（`python web/server.py` 打开 `http://127.0.0.1:8765`），**无路由、无导航栏、无 Tab**；模块即卡片，自上而下按使用顺序线性排布。命令行（CLI）与网页共用同一套核心流程，能力对等（见 §6）。
- **页面框架**：渐变头部（标题+一句话说明）→ 主区 6 张卡片（§3）→ 页脚（免责声明）。
- 单任务模型：同一时刻只允许一个提取或迁移任务运行，运行中另一入口按钮禁用。

## 3. 首页的内容结构

本产品只有一个页面（即首页），自上而下：

| 序 | 卡片 | 显示条件 |
| --- | --- | --- |
| 1 | 提取设置 | 常显 |
| 2 | 迁移旧字幕 | 常显 |
| 3 | 运行进度 | 有任务时显示 |
| 4 | 结果汇总 | 任务结束时显示 |
| 5 | 审查报告 | 点「预览第 1 集」后显示 |
| 6 | 生成的文件 + 预览 | 任务结束/预览后显示 |

## 4. 每个模块的用途

1. **提取设置**：一切提取行为的入口——选模式、给来源、给登录态、决定输出去哪。
2. **迁移旧字幕**：把旧格式字幕文件夹原地转换成新格式笔记写入 vault 的入口（不联网）。
3. **运行进度**：逐行实时日志，让长合集提取过程可感知、可判断是否触发风控。
4. **结果汇总**：三分类结果（成功/无字幕/失败）+ 落点信息 + 直达 Obsidian。
5. **审查报告**：批量提取前预览第 1 集排版与清洗情况，避免几十集跑完才发现格式问题。
6. **生成的文件 + 预览**：点开任意产物阅读渲染效果或原文，替代去文件管理器翻找。

## 5. 每个模块第一版最必要的数据和操作

### 5.1 提取设置

| 数据 | 说明 |
| --- | --- |
| 模式 | 单选：离线演示 / 真实接口（演示模式隐藏 Cookie 与「输出位置」区块） |
| 来源 | 合集页 URL（含 `sid=`）、合集内任一视频链接或 BV 号、多P视频链接、纯数字 season_id |
| Cookie | 真实接口必填（至少 SESSDATA）+「检测登录态」按钮；仅本次运行透传 |
| 输出位置 | 单选：**写入 Obsidian vault（默认）** / 普通文件夹。vault 模式：vault 路径 + vault 内字幕文件夹（默认 `B站字幕`，可嵌套如 `学习/B站字幕`）；普通文件夹模式：沿用「输出目录」 |

| 操作 | 反馈 |
| --- | --- |
| 「检查 vault」 | 三态：✅ 可写且找到 `.obsidian`；⚠️ 可写但非 vault 根（「打开」链接不可用）；❌ 不存在/不可写（提供「创建该文件夹」） |
| 「预览第 1 集」 | 审查报告 + 按新格式（含属性头）渲染第 1 集 |
| 「开始提取」 | 进度→汇总→文件列表；vault 路径为空/不存在时阻止并给出指向性提示 |

### 5.2 迁移旧字幕

| 数据 | 说明 |
| --- | --- |
| 旧字幕目录 | 默认仓库 `output/`；其下每个含 `.md` 的子文件夹视作一个合集 |
| 合集勾选 | 「扫描旧合集」后列出合集名+文件数，默认全选 |
| 覆盖开关 | 默认跳过已迁移笔记（识别本工具写入的 `fetched_by: subtitle-cli` 标记）；勾选后重写 |

操作：「扫描旧合集」「开始迁移」（目标用上方配置的 vault）。迁移不联网，BV 号/链接恢复不了就留空并在报告说明。

### 5.3 运行进度 / 结果汇总

- 进度日志在现有行基础上新增：`已写入 vault：EP01 xxx.md`、`索引页已更新：《合集名》.md（N 集）`。
- 汇总新增落点行 + **「在 Obsidian 中打开合集索引」** 按钮（`obsidian://open?vault=<名>&file=<字幕文件夹>/<合集名>/<合集名>`；vault 检查为 ⚠️/❌ 时隐藏）。

### 5.4 生成的文件 + 预览

- 文件列表：索引页固定第一并默认选中；每文件带徽标（新写入/跳过/迁移）。
- 预览：渲染视图把 YAML 属性头画成属性表、索引页双链可点击跳预览；「查看原文」显示含 YAML 全文。

## 6. 首页、今日计划和各模块之间的关系

> 说明：本产品是单页工具，**不存在「今日计划」模块**——PRD 模板中的该项按实际产品结构如实映射；若确需「每日计划」类功能（如计划看哪些合集、关联笔记），属新增需求，另行确认后再入版。

实际关系结构：

- **首页（唯一页面）= 全部模块的容器**：设置卡决定「去哪、怎么取」，进度/汇总/文件卡呈现「取到了什么」，迁移卡是独立的第二条数据入口。
- **CLI 与网页同核**：`subtitle-cli <来源> --vault …` 与网页提取同一管线；`subtitle-cli-migrate` 与迁移卡同一管线。任一入口写入的笔记格式完全一致，可混用。
- **数据流**：来源 → 解析合集 → 逐集取字幕 → 转换（清洗/拼段）→ 加属性头 → 写入 `<vault>/<字幕文件夹>/<合集名>/` → 重生成索引页 → 汇总/预览/Obsidian 打开。
- **vault 是唯一长期数据存放地**：工具自身不留存笔记数据（只留 §7 的配置），卸载工具不影响知识库。

## 7. 本地数据文件、备份和恢复要求

| 文件 | 内容 | 要求 |
| --- | --- | --- |
| `<vault>/<字幕文件夹>/<合集名>/*.md` | 分集笔记 + 索引页 | **唯一有价值数据**，归用户 vault 自身的备份/同步体系管理（Obsidian 同步等），工具不另做备份 |
| `~/.subtitle-cli/config.json` | vault 路径、字幕文件夹 | 可随时删除重建（重填即可）；**绝不写入 Cookie 等敏感信息** |
| Cookie | — | 永不落盘、不进日志、不进 Git（现状不变） |
| `output/`、`demo_output/` | 运行产物 | 已在 `.gitignore`，不进 Git |

**写入纪律（恢复能力的来源）**：

1. 工具对既有用户文件**只新增、只更新（索引页重生成）、永不删除**；唯一覆盖路径是迁移时显式勾选「覆盖已迁移」。
2. 任何中断（断网、风控、关进程）后，重跑同一命令即恢复：已成功分集自动跳过，失败分集续传。
3. 迁移可重复执行：默认跳过已迁移笔记，`--overwrite` 可整体重做。

## 8. 第一版必须完成的功能

| # | 功能 | 要点 |
| --- | --- | --- |
| F1 | vault 直写 | CLI `--vault`（`--vault-subdir`）+ 网页输出位置；路径持久化到配置文件，网页回填 |
| F2 | 分集属性头 | `title / collection / season_id / bvid / episode / url / fetched_at / fetched_by / tags`（键名英文，值中文；多P 的 `url` 带 `?p=N`；正文一字不动） |
| F3 | 合集索引页 | `<合集名>.md` 置于合集文件夹内，`type: index` 属性头 + 全部分集双链；每次运行重生成 |
| F4 | 旧字幕迁移 | 网页迁移卡 + `subtitle-cli-migrate`（`--dry-run` / `--overwrite`）；不联网；缺字段留空并报告 |
| F5 | vault 自检 | 「检查 vault」三态 + 创建文件夹动作；`/api/check-vault` |
| F6 | Obsidian 直达 | 汇总卡「在 Obsidian 中打开合集索引」按钮 |
| F7 | 预览新格式 | 「预览第 1 集」含属性头；审查报告含属性头字段完整性 |
| F8 | 既有能力保留 | 增量跳过、三类汇总与退出码（0/1/2）、清洗无效行、登录态检测、风控退避、演示模式离线可用 |

## 9. 第一版暂时不做的功能

- 其他平台（YouTube、抖音等）、b23.tv 短链；
- 无字幕视频的语音转写（ASR）；
- 时间轴、翻译、Markdown 以外格式；
- 段落拼接质量深度打磨；
- Obsidian 插件化（产物始终是普通 Markdown 文件）；
- 迁移时联网补齐缺失字段（BV 号等）；
- 笔记模板自定义、双链以外的图谱增强；
- 弹窗选目录、安装包等包装；
- 「今日计划」类计划管理功能（本产品无此概念，见 §6）。

## 10. 验收标准

以下各条均可人工或自动化实际检查（【T】标注的可进自动化测试）。

**提取入库（F1/F2）**

1. 真实模式 + 有效来源与 Cookie + 有效 vault 路径，提取结束后：`<vault>/<字幕文件夹>/<合集名>/` 下存在全部成功分集的 `EPNN 标题.md`；每个文件以 `---` YAML 开头且含 §8-F2 全部字段；正文一级标题与段落与现有转换结果一致；多P 视频的 `url` 含 `?p=N`。【T】
2. 演示模式回归：无需 Cookie，不出现 vault 区块，输出到 `demo_output/`，与现状一致。【T】
3. 同一来源重复运行：第二次日志含「跳过」，已存在文件内容与修改时间不变，索引页仍收录全部分集。【T】
4. vault 路径为空/不存在时「开始提取」被阻止并给出指向性提示；「检查 vault」对 ✅/⚠️/❌ 三态输出正确；❌ 时可用「创建该文件夹」后转为可写。【T】

**索引页与直达（F3/F6）**

5. 提取/迁移结束后合集文件夹内存在 `<合集名>.md`：`type: index` 属性头，双链条目数 = 文件夹内分集笔记数；无字幕/失败集不入索引，但历史已存在的分集笔记会被收录。【T】
6. vault 根目录有效时「在 Obsidian 中打开」能定位到该索引页；检查为 ⚠️/❌ 时按钮不显示。【T】

**迁移（F4）**

7. 对含 N 个旧格式 `.md` 的合集迁移：生成 N 个带属性头笔记 + 索引页；BV 号/链接为空，报告列出缺失字段；合集名/序号无法解析的散文件不参与并计入说明。【T】
8. 重复迁移默认全部「跳过」；勾选覆盖后重写且正文仍与原字幕一致。【T】
9. 断网环境下迁移可完整完成。【T】
10. `subtitle-cli-migrate --dry-run` 只打印计划不写盘。【T】

**CLI（F1/F4）**

11. `subtitle-cli <来源> --vault "…" [--vault-subdir "…"]` 与网页行为一致，嵌套子文件夹生效；不带 `--vault` 时旧用法与退出码 0/1/2（中断 130）完全不变。【T】

**安全与数据纪律（§7）**

12. Cookie 不出现在日志、生成文件、配置文件中。【T】
13. 全流程不删除、不移动用户既有文件；唯一覆盖是显式勾选覆盖迁移。【T】
14. `/api/file` 无法读取当前任务目录（含 vault 模式）之外的文件。【T】

**回归**

15. `pytest` 全绿（含 F1–F4 新增用例）；`python demo/run_demo.py` 端到端可跑通。
